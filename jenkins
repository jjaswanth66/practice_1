pipeline {
    agent {
        docker {
            // Define the Docker image you want to use
            image 'node:16-alphine'
            // Other configuration options like registry credentials, etc., can be added here
        }
    }
    stages {
        stage('test') {
            steps {
                sh 'node --version'
                    
                    }
                    
                }
            }
        }
        // Add more stages as needed
  
